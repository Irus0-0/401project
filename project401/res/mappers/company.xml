<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="company">
    <resultMap type="company.vo.CompanyVO" id="companyVO">

    </resultMap>

    <insert id="insert" parameterType="company.vo.CompanyVO">
        insert into customer (customer_id, customer_pw, name, phone_num,
                            gender, birthdate, addr, grade, sanctions_count)
        values ('${customerId}', '${customerPw}', '${name}', '${phoneNum}',
                '${gender}', to_date('${birthdate}','yyyy-MM-dd'), '${addr}', 0, 0)
    </insert>

    <select id="login" resultMap="companyVO">
        select *
        from customer
        where customer_id = #{customerId}
          and customer_pw = #{customerPw}
    </select>

    <delete id="delete" parameterType="company.vo.CompanyVO">
        delete from customer
         where customer_id = #{customerId}
    </delete>

    <select id="isExist" resultMap="companyVO">
            select *
              from customer
             where customer_id = #{customerId}
    </select>

</mapper>

